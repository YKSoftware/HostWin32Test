<YK:Window x:Class="HostWin32Test.Views.MainView"
           xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
           xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
           xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
           xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
           xmlns:YK="clr-namespace:YKToolkit.Controls;assembly=YKToolkit.Controls"
           xmlns:vw="clr-namespace:HostWin32Test.Views"
           mc:Ignorable="d"
           Title="MainView"
           Width="800" Height="600"
           IsClosingConfirmationEnabled="False">
  <StackPanel>
    <TextBlock Text="{Binding WindowHandle}" />
    <ContentControl x:Name="win32ContentHost" Width="260" Height="260" />
    <TextBlock Text="{Binding Counter, StringFormat='{}Counter = {0}'}" />
    <TextBlock Text="{Binding WindowHandleFromCpp}" />
    <Button Content="Read Window Handle From Cpp" Command="{Binding ReadCommand, Mode=OneTime}" />
    <Button Content="Write Window Handle To Cpp" Command="{Binding WriteCommand, Mode=OneTime}" />
  </StackPanel>
</YK:Window>
